spring:
  application:
    name: Config-Dev
eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch_registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
